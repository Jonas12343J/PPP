#include "project.h"

int main() {
    //Podemos guardar sempre o previous antes de um delete
    /*
     * Menu
     * 1 - Reservar (se nao houver vaga, vai par a lista de espera)
     * 2 - Cancelar reserva
     *      2.1 - Reserva -> se houver uma reserva em espera nesse horário, fica nesse spot
     *      2.2 - Pré-reserva
     * 3 - Listar tudo (mais antigas primeiro -> por ordem de reserva)
     * 4 - Listar por cliente (mais recentes primeiro)
     */

    return 0;
}

